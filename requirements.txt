# Note that this requirements file list the requirements needed to run the testing suite.
# Package dependencies should be listed in setup.py instead. 
pytest
pandas  # unpinning pandas makes binder work but the tests break
python-louvain==0.11
numpy
scipy
networkx>=2.2
seaborn
forceatlas2
nilearn==0.5.0a0
